def save_user(**user_info):
    print(user_info)


save_user(id=1, name="John", age=30, email="john@example.com")

# when we use double asterisks (**)
# we can pass multiple key_value pairs or multiple keyword arguments to a function.
