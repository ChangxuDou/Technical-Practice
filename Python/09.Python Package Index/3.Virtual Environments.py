# if we have another project,in that we want to install a early version of the package

# To solve the problem: create a virtual environment and install the package

# use command"python3 -m venv env" on the terminal
# on the file explore panel ,we have a new directory: env
# activate the environment, use command "source env/bin/activate"
# deactivate the environment,use command "deactivate"
