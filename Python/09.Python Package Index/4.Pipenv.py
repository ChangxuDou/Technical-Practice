# on the terminal ,we try these commands

# command"pip3 install pipenv"
# command"pipenv install requests"
# comand"pipenv --venv" to see the path of virtual directory
# command"pip3 uninstall requests"
# you will get wrong when you run "python3 2.Pip.py",cuz there is no Moduel requests

# command"pipenv shell" to open virtual environment
# then we run ageain "python3 2.Pip.py"
# now we are in virtual environment , if you want to deactivate, run "exit" to quit
